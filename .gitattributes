# Normalise line endings so the repo stays LF regardless of the OS it is
# cloned on. Without this, Windows checkouts commit CRLF back and churn diffs.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
